"""Tests for bot/. Run them from bot/ with:

    python -m unittest discover -s tests -t .

Nothing here needs a Slack workspace, a network connection, or the Claude
Code CLI: the Slack client and the headless session are both injected, so
every branch is reachable from a stand-in. Every name and link in the
fixtures is invented, per the repository's fixture rule.
"""
